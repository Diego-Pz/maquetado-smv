// jQuery ya está cargado en tu proyecto
$(function () {
  // Ejemplo de interacción: captura el “Ver más”
  $('.ea-card').on('click', function (e) {
    // reemplaza con navegación real por categoría
    // window.location.href = this.href;
    e.preventDefault();
  });
});
